<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GithubUser" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/grey_bg</item>
        <item name="colorPrimaryVariant">@color/black_bg</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:windowBackground">@color/black_bg</item>
    </style>

    <style name="TextPrimaryBold" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="TextPrimary" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white_text</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="TextSecondary" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/grey_text</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>